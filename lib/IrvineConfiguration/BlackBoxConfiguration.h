#pragma once

class BlackBoxConfiguration
{
public:
    uint32_t interval = 0u;
    // Define an array or vector for parameters if needed, or individual parameters
    // For example, if parameters are just integers or strings, they can be defined as:
    uint32_t parameter1 = 0u;
    uint32_t parameter2 = 0u;
    // Add more parameters as needed
};
